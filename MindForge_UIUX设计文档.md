# MindForge（心智锻造）UI/UX 设计文档

> 版本：v2.0（风格重构 · 对齐 Caoch 轻量极简专业风）
> 设计语言：**轻量极简专业风**（参考 Caoch 商务教练 / 咨询类 Framer 模板）—— 浅中性暖调主色、非黑墨色、单一低饱和强调色
> 配套文档：`MindForge_PRD.md`、`MindForge_产品需求验证报告.md`、`MindForge_技术方案_AI技术选型.md`
> 适用范围：单页 Web 前端（聊天式界面）+ FastAPI 后端，本期纯文本交互
> 强制约束：**不使用纯黑 `#000000` 作为主色或大面积底色**

---

## 0. 风格重构决策记录

| 项 | 旧版（v1.0） | 本次重构（v2.0） |
|---|---|---|
| 参考系 | Attio 极简科技风 | Caoch 商务教练 / 咨询 Framer 模板 |
| 主色调 | 暗色优先，墨黑 `#111111` 主色 | 浅中性暖调 `#F7F5F2` 主色 |
| 墨色 | `#111111` 近黑 | `#2B2B2B` / `#33312E` 深石板灰（非纯黑） |
| 强调色 | 无品牌色 | 单一低饱和强调色（深青绿 `#1F4E4A`） |
| 气质 | 冷静、专注、克制 | 可信赖、专业、高端、温暖克制 |

**重构理由**
1. 教练 / 咨询类产品以"可信赖、专业、高端"为先，浅底更易传达安定与权威感。
2. 高压脱敏场景需要视觉安定感，浅暖底比暗底更减压、更贴近"赋能而非评判"的产品立场。
3. 沿用 Caoch「少即是多」路线：靠留白、大字号排版与真实人物影像建立权威，而非装饰堆叠。
4. 满足明确约束：**严禁纯黑作主色 / 大面积底色**，最深墨色止于 `#2B2B2B`。

**强调色选型**：采用**深青绿 `#1F4E4A`** 作为唯一强调色——低饱和、沉稳、传达"信任 + 专业 + 冷静"，与心理脱敏场景的心理暗示一致；面积控制在 10% 以内（仅 CTA、关键数据、激活态）。

---

## 1. 设计原则与风格概述

### 1.1 设计哲学
MindForge 是面向求职 / 考研复试的高压表达力 AI 训练工具。UI 不靠插画、渐变或装饰色取胜，而靠「真实的训练对话界面本身就是视觉主角」来建立信任。用户在高压场景下需要的是**安定感而非刺激感**——与产品核心价值（高压心理脱敏）在体验层面同频：**界面越是克制、底色越是温暖明亮，用户越能专注训练本身**。

### 1.2 四重设计原则
| 原则 | 含义 | 落地表现 |
|---|---|---|
| **可信赖（Credible）** | 专业、权威、靠谱 | 浅暖底 + 大字号排版 + 真实人物摄影，非卡通插画 |
| **高端（Premium）** | 克制、精致、有质感 | 单一强调色、细描边、极轻阴影、大留白 |
| **温暖克制（Warm-but-restrained）** | 建立信任，不过度抚慰 | 中性无衬线 + 建设性文案 + 教练侧栏支持性语气 |
| **安定（Calm）** | 降低焦虑，专注训练 | 浅主色、低对比、禁用纯黑与强刺激色块 |

### 1.3 风格依据（Caoch 设计语言迁移）
MindForge 与 Caoch 同为"让人信赖的专业服务"流派：
- **浅色主导**：整页以暖白 / 米白为底，墨色仅用于文字，营造明亮、开放的训练环境。
- **大字号排版（Big Type）**：Hero 与页脚使用超大标题建立记忆点；页脚用"Big Type"标语强化品牌。
- **真实人物影像（People）**：Hero 与信任背书区使用真实教练 / 学员摄影，传递"真人陪伴"的信任感。
- **滚动视差与微动效**：Hero 产品浮层轻微视差、元素入场淡入上移，克制不浮夸。
- **内容即主角**：聊天训练界面浮层直接作为首屏右侧主视觉，零设计成本、高可信度。

### 1.4 与产品定位的对冲设计
PRD 定位「心理韧性 + 表达力训练器，不做招聘决策」。UI 必须体现这一立场：
- 面试官冷峻（高压档的"压力感"由文案与节奏承担，而非红色警示色渲染）。
- 教练侧栏恒定以「建设性 + 支持性」语气对冲，视觉上用低饱和语义色区分二者。
- 安全护栏可见且低焦虑，时刻提示「你随时可以暂停、降级、结束」——把控制权交还用户。

---

## 2. 色彩系统（含色板与禁用黑主色说明）

### 2.1 色彩铁律
- **严禁使用纯黑 `#000000` 作为主色或大面积底色**。
- 最深墨色止于 **`#2B2B2B`**（深石板灰），仅用于文字与极少数高对比元素。
- 主视觉色调为**浅中性暖调**，大面积区域（页面底、分区底）一律使用浅色。
- 强调色**仅一种**（深青绿 `#1F4E4A`），面积 ≤ 10%，仅用于 CTA、关键数据、激活态。
- 整体明度对比满足 **WCAG AA**（正文 ≥ 4.5:1，大字 ≥ 3:1）。

### 2.2 基础色板（Token）
| Token | 色值 | 用途 |
|---|---|---|
| `--paper-bg`（主背景） | `#F7F5F2` | **页面主导视觉色调**（暖白 / 米白） |
| `--surface`（卡片底） | `#FFFFFF` | 卡片、浮层、输入框底 |
| `--surface-alt`（次表面） | `#F1EFEC` | 分区底色、弱浮层、hover 态 |
| `--ink-900`（主墨色·非黑） | `#2B2B2B` | 标题、主文字、关键强调文字 |
| `--ink-700`（正墨色·非黑） | `#33312E` | 正文、气泡文字 |
| `--ink-500`（次文字） | `#6F6A62` | 辅助说明、未选中控件 |
| `--ink-400`（弱文字） | `#9A958D` | 占位符、三级提示（来自规范） |
| `--line`（分割线 / 边框） | `#E8E5E1` | 1px 细边框、分隔线（来自规范） |

### 2.3 强调色（唯一，低饱和）
| Token | 色值 | 用途 |
|---|---|---|
| `--accent`（深青绿） | `#1F4E4A` | 主 CTA 填充、关键数据、激活态、链接 |
| `--accent-700` | `#163831` | 强调色 hover / 按下态 |
| `--accent-tint` | `#E4ECEA` | 强调色浅底（chip、选中背景、浅高亮） |

> 强调色用量克制：按钮填充、数据高亮、激活指示三类场景；不得铺成大色块背景。

### 2.4 语义色（仅功能性点缀，低饱和，来自界面内真实内容）
| Token | 色值 | 用途 |
|---|---|---|
| `--lvl-easy`（低饱和绿） | `#6B8E6B` | 温和档指示 |
| `--lvl-std`（低饱和蓝灰） | `#6B7C8E` | 标准档指示 |
| `--lvl-hard`（低饱和琥珀） | `#B08A4E` | 高压档指示 |
| `--metric-good`（绿） | `#5C8A5C` | 填充词 / 停顿指标良好 |
| `--metric-warn`（黄） | `#C2A24B` | 指标临界 |
| `--metric-bad`（红） | `#B06B5C` | 指标提示（仅提示，不渲染恐慌） |

> 语义色仅用于档位状态点、指标阈值色点等小面积指示，不得大面积铺色；且均配文字 / 图标双重指示，不依赖颜色单一通道。

### 2.5 明度模式说明
- **本期仅交付浅色主题**（主背景 `#F7F5F2`）。不提供暗色默认模式，以满足"禁用纯黑主色"的硬约束。
- 若未来评估暗色模式，必须同样规避纯黑：底色改用深石板灰 `#2B2B2B` 家族，文字改用 `#F7F5F2` 暖白，强调色与语义色映射保持不变。

---

## 3. 字体与排版规范

### 3.1 字体族
- **无衬线（Sans Serif）**：优先 `Inter` / `Söhne` / `Helvetica Neue` 类现代几何无衬线；中文回退 `思源黑体 / 系统无衬线`。
- 全站统一一套字体族，不混搭衬线，保持极简一致。

### 3.2 字阶与用法
| 角色 | 字号 | 字重 | 行高 | 字距 | 颜色 |
|---|---|---|---|---|---|
| Hero 超大标题（Big Type） | 56–72px | 700–800 | 1.15 | -0.02em | `--ink-900` |
| 区块标题（H2） | 32–40px | 700 | 1.25 | -0.01em | `--ink-900` |
| 卡片 / 小节标题（H3） | 20–24px | 600 | 1.3 | 0 | `--ink-900` |
| 正文 | 16–18px | 400 | 1.6–1.7 | 0 | `--ink-700` |
| 辅助说明 | 13–14px | 400 | 1.4 | 0 | `--ink-500` |
| 对话气泡文字 | 15–16px | 400 | 1.5 | 0 | `--ink-700` |
| 指标数字 | 14–16px | 600（等宽数字） | 1.2 | 0 | `--ink-900` / `--accent` |
| 页脚 Big Type 标语 | 40–56px | 700–800 | 1.15 | -0.02em | `--ink-900` |

### 3.3 排版原则
- **层级靠字号、字重、颜色明度拉开**，而非靠描边或阴影。
- 文本默认**左对齐**；Hero 与页脚 Big Type 允许左对齐大标题建立视觉气势。
- 正文行高 1.6–1.7，段落间距 ≥ 8px，保证浅底上的可读呼吸感。
- 指标数字使用等宽数字（`font-variant-numeric: tabular-nums`），对齐更稳。

---

## 4. 页面结构与布局栅格

### 4.1 信息架构（单页 Web，无多路由跳转）
```
MindForge（单页）
├── 导航栏（常驻细条 · 浅底）
│   ├── Logo「MindForge 心智锻造」
│   ├── 中部：产品定位锚文（训练工具 · 非招聘决策）
│   └── 右侧：帮助中心 / 数据管理（无主题切换，仅浅色）
├── Hero 区（首屏 · 浅暖底 + 轻微视差）
│   ├── 左：Big Type 定位 + 一句解释 + 双 CTA（开始训练 / 了解机制）
│   └── 右：真实训练对话界面浮层（真实人物摄影点缀）
├── 信任背书 / 数据区（浅底 + 大留白，关键数据用强调色）
├── 能力 / 服务卡片区（圆角轻卡片，细边框）
├── 案例 / 效果区（真实学员影像 + 引述）
├── 训练区（进入后占满视口）
│   ├── 左：主对话区（场景/档位选择 → 面试官↔用户）
│   ├── 右：教练侧栏（实时指标 + 1–3 条建议）
│   └── 底：输入框 + 安全护栏常驻控件（暂停/降级/结束）
├── 结案报告（覆盖层 / 全屏，结束后出现）
├── 页脚（Big Type 标语 + 联系方式 + 隐私说明）
└── 帮助中心（抽屉 / 弹层）
```

### 4.2 页面流（核心）
```
首屏(Hero) → [开始训练] → 场景选择卡（考研复试/校招面试）
   → 档位选择（温和/标准/高压）→ [进入模拟]
   → 训练区（面试官开场 → 用户作答 → 教练建议 循环）
   ├── [暂停] → 呼吸引导浮层 → [恢复]
   ├── [降级] → 二次确认 → 切温和 + 放缓指令注入
   ├── [结束] → 二次确认 → 结案报告
   └── 高压反噬信号 → 弹窗建议降级/暂停 → 用户确认
   → 结案报告（分析师生成 < 10s）→ 导出/分享/返回首屏
```

### 4.3 布局栅格
- **容器最大宽 1200px**，居中；桌面两侧留白 64–128px，窄屏收敛为 16–24px。
- **基线间距**：8px 倍数体系（8 / 16 / 24 / 32 / 48 / 64 / 96）。**section 间距 ≥ 96px**，保证大留白节奏。
- **Hero 双栏**：左文案 50% + 右产品浮层 50%；右栏随滚动轻微视差。
- **训练区双栏**：左主对话 ~65% + 右教练侧栏 ~35%；底栏常驻输入与安全护栏。
- 圆角体系：控件 `999px`（pill）、卡片 `12px`、浮层 `16px`。
- 阴影：极轻 `0 2px 8px rgba(0,0,0,0.04)`；禁用重投影与粗边框。

---

## 5. 核心组件库（按钮 / 卡片 / 导航 / 表单 + 训练专属）

### 5.1 主 CTA（Primary Button）
- 样式：深青绿 `--accent` 填充 + 白字 + 完全圆角（pill，`border-radius: 999px`）。
- 用途：「开始训练」「结束会话」「导出报告」等核心动作。
- Hover：`--accent-700` 提深 / `transform: scale(0.98)`，过渡 200–400ms ease-out。

### 5.2 次要按钮（Ghost / Secondary）
- Ghost：透明底 + `--ink-500` 1px 描边 + 同色文字；用途「了解机制」「再来一次」「返回首页」。
- 文字按钮：仅 `--accent` 文字 + 下划线悬停；用途「查看机制」「数据管理」。

### 5.3 导航栏
- 极简细条（高度 ~56px），`--paper-bg` 底 + 底部 1px `--line` 细边框。
- Logo 左置（墨色文字，非黑），中部定位锚文（`--ink-500` 小字），右侧操作（帮助 / 数据）。

### 5.4 卡片 / 面板
- 白底 `--surface` 或 `--surface-alt` + 极轻阴影 `0 2px 8px rgba(0,0,0,0.04)` + 圆角 12px。
- 1px `--line` 细边框可选；避免粗边框与重阴影。
- 服务 / 能力卡片：图标 + 标题 + 一行说明，hover 轻微上浮（translateY -2px）。

### 5.5 表单 / 输入框
- 浅底 `--surface` + 1px `--line` 边框 + 圆角 12px；聚焦态边框转 `--accent`。
- 占位符 `--ink-400`；禁用纯黑边框。
- 文本域（训练作答）：多行、舒适内边距，回车发送 / Shift+回车换行。

### 5.6 施压档位选择器（三档分段控件）
```
┌──────────────────────────────────────┐
│ 选择施压档位                          │
│ [ 温和 ] [ 标准 ] [ 高压 ]            │
│  绿      蓝灰      琥珀（选中态填充）  │
│ 温和：多鼓励 · 标准：适度质疑        │
│ 高压：冷峻追问（绝不人身攻击）        │
└──────────────────────────────────────┘
```
- 未选态浅灰底 `--surface-alt`，选中态用语义低饱和色（`--lvl-easy/std/hard`）填充 + 白字。
- 每段下方一行解释，明确「高压 ≠ 侮辱」的边界；切换有 200–400ms 滑块过渡。

### 5.7 安全护栏控件（暂停 / 降级 / 结束）
- 三个常驻按钮，低视觉权重（弱描边 / `--ink-500` 文字），始终可见。
- 暂停：瞬时切换（无确认）→ 柔和浅色遮蔽层 + 呼吸引导。
- 降级 / 结束：二次确认弹层（浅色卡片），避免误触。
- 反噬弹窗：高压档命中外露焦虑 / 退出信号 → 支持性浅色弹窗「看起来有点紧，需要降低强度吗？」。

### 5.8 对话气泡
- 面试官：左对齐，`--surface` 底 + 1px `--line` 细边框浮层。
- 用户：右对齐，`--accent-tint` 浅青绿底 + `--ink-900` 文字（非黑，柔和区分）。
- 流式逐字上屏（首字 < 2s），生成中显示光标 / 三点加载。

### 5.9 教练侧栏（指标 + 建议）
- 指标区：3 个指标行（填充词频次 / 平均句长 / 停顿·语速），每条含数字 + 阈值色点（绿/黄/红，配文字等级）。
- 建议区：每轮 1–3 条，建设性 + 支持性语气，与面试官冷峻对冲。
- 信任标识：底部常驻「训练工具 · 本地处理」轻量标识（隐私 by design 可见化）。

### 5.10 结案报告卡（覆盖层 / 全屏）
- 四维评分（语言组织 / 逻辑清晰 / 情绪稳定 / 改进建议）用条形 + 阈值色点，单色 + 语义色，不用彩色填充。
- 本地填充词统计作为客观佐证并列展示；报告可导出（本地生成，不上传）。
- 操作：`[ 导出报告 ]`（主 CTA）`[ 再来一次 ]`（ghost）`[ 返回首页 ]`（文字）。

### 5.11 隐私标识
- 轻量标识「本地处理 · 数据可删除」，常驻教练侧栏底部与帮助中心，传递隐私 by design。

---

## 6. 影像与图标规范

### 6.1 影像策略（People · 真实优先）
- **优先使用真实人物 / 场景摄影**（教练、学员、训练场景），传递"真人陪伴"的信任感；**避免插画化卡通**。
- Hero 右侧与信任背书区可放置真实学员 / 教练肖像（暖光、自然、专业感）。
- **背景大图**用于 Hero 与分区过渡，叠加**浅色蒙版**（如 `rgba(247,245,242,0.82)`）保证文字可读，绝不压暗成黑底。

### 6.2 图标规范
- **线性细描边图标**，统一 **1.5px** 描边，圆角端点。
- 颜色跟随 `--ink-700` 或 `--accent`；禁用实心重图标与彩色块图标。
- 图标集需覆盖：场景（复试 / 面试）、档位（温和 / 标准 / 高压）、护栏（暂停 / 降级 / 结束）、指标（填充词 / 句长 / 语速）、导出 / 分享 / 帮助。

### 6.3 视觉素材清单（建议）
- Hero 主视觉：训练对话界面浮层 + 真实教练 / 学员肖像。
- 信任区：数据大数字（强调色）+ 真实学员引述头像。
- 案例区：真实学员前后对比引述 + 人像。

---

## 7. 动效规范

### 7.1 总体原则
- 克制、自然，**动画时长 200–400ms，缓动 `ease-out`**；禁用弹跳、强闪、长时长动画。
- 尊重 `prefers-reduced-motion`：关闭逐字 / 视差，改用整段淡入。

### 7.2 滚动视差（Hero 区）
- 产品浮层 / 人像随滚动有轻微视差层次，幅度克制，不喧宾夺主。

### 7.3 入场与微交互
- 元素入场：淡入 + 上移（translateY 8→0），200–400ms ease-out，错落出现。
- 按钮 / 卡片 hover：轻微缩放或上浮（translateY -2px），过渡 200–400ms。
- 档位切换：滑块过渡 250ms ease-out。

### 7.4 对话流
- 流式逐字上屏（首字 < 2s），保持阅读节奏；生成中光标 / 三点加载，避免空白焦虑。

### 7.5 安全护栏动效
- **暂停**：半透明浅色遮蔽层淡入（200ms）+ 呼吸圆圈动画（4s 循环，提示深呼吸）+ 安抚语。
- **降级**：档位指示滑块回退（250ms）+ 文案「强度已降低」。
- **结束**：对话区温和收束（淡出 300ms）+ 报告覆盖层淡入。

### 7.6 异常反馈（友好、非恐吓）
| 场景 | 反馈形式 | 文案示例 |
|---|---|---|
| 未选场景 / 档位 | 内联提示 + 阻止进入 | 「请先选择场景 / 施压档位」 |
| 空提交 | 输入框微震 + 提示 | 「请输入内容后提交」 |
| AI 异常 / 中断 | 非阻断横幅 + 重试 | 「生成出错，请重试」 |
| 网络超时 | 保留输入 + 提示 | 「网络超时，请检查网络后重试」 |
| 超长会话 | 无感压缩提示 | 「已为你精简上下文，继续训练」 |
| 施压反噬 | 支持性弹窗（浅色） | 「看起来有点紧，需要降低强度吗？」 |

---

## 8. 响应式策略

### 8.1 断点与布局
| 断点 | 布局 |
|---|---|
| **桌面（≥1024px）** | 左主对话 + 右教练侧栏双栏；Hero 双栏（文案 50% / 浮层 50%）；安全护栏底栏常驻 |
| **平板（768–1023px）** | 教练侧栏折叠为可展开抽屉（默认收起）；Hero 可改为单栏堆叠，浮层下移 |
| **移动（<768px）** | 单栏；教练侧栏转底部抽屉 / 折叠卡；档位分段控件自适应；安全护栏悬浮于底部；Hero Big Type 缩放至 40–48px |

### 8.2 一致性要求
- 移动端**保持同样的留白节奏与字号比例**（section 间距按比例收敛，不取消留白）。
- 容器最大宽 1200px；窄屏两侧留白收敛为 16–24px。
- 所有断点下均使用浅主色，严禁在移动端切到纯黑底。

### 8.3 无障碍（A11y）
- 语义色均配文字 / 图标双重指示（档位除色点外有文字标签；指标红/黄/绿旁附文字等级）。
- 所有可交互元素可达键盘焦点，焦点环可见（2px `--accent` 描边）。
- 安全护栏确认弹窗为 `role="alertdialog"`，屏幕阅读器优先播报。
- 对比度满足 WCAG AA（正文 ≥ 4.5:1，大字 ≥ 3:1）。

---

## 附录 A：设计锚点提示词（全链路复用）
> 高保真原型生成、AI 视觉生成、设计评审统一口径。改此一处即可全局生效。

```
你正在设计 MindForge（心智锻造）——面向求职 / 考研复试的高压表达力 AI 训练 Web 应用。
请采用「轻量极简专业风（参考 Caoch 商务教练 / 咨询 Framer 模板）」：

【配色 · 禁用纯黑主色】主背景浅中性暖调 #F7F5F2；卡片 #FFFFFF / #F1EFEC；
  墨色 #2B2B2B / #33312E（非黑）；分割线 #E8E5E1；次要文字 #9A958D。
  唯一强调色：深青绿 #1F4E4A（仅 CTA / 关键数据 / 激活态，面积 ≤ 10%）。
  语义点缀：温和绿 #6B8E6B / 标准蓝灰 #6B7C8E / 高压琥珀 #B08A4E；指标绿黄红低饱和。
  严禁纯黑 #000000 作主色或大面积底色；明度对比满足 WCAG AA。
【字体】Inter / 思源黑体 无衬线；Hero 与页脚 Big Type 超大字号 700–800；
  正文 16–18px / 行高 1.6–1.7 / 字重 400；指标数字等宽。
【布局】单页；Hero 双栏（文案 + 真实对话浮层，轻微视差）；训练区左对话 65% + 右教练侧栏 35%；
  大留白（section ≥ 96px）；容器最大宽 1200px；圆角 12 / 浮层 16 / 控件 999px；极轻阴影 0 2px 8px rgba(0,0,0,0.04)。
【影像】真实人物 / 场景摄影优先，避免卡通插画；Hero 背景大图叠加浅色蒙版保证可读。
【图标】线性细描边 1.5px，颜色跟随墨色或强调色。
【动效】200–400ms ease-out；滚动视差、入场淡入上移、hover 微交互；克制，禁用弹跳强闪。
【气质】可信赖、专业、高端、温暖克制——让真实训练对话成为视觉主角。
【反例】不要纯黑主色、不要插画卡通、不要鲜艳色块、不要强对比渐变、不要玻璃炫光、不要拟物重阴影。
```

## 附录 B：与 FastAPI 单模型多角色技术方案的衔接
| UI 元素 | 后端 / 模型职责 | 前端职责 |
|---|---|---|
| 场景 / 档位选择 | 档位变量注入面试官 prompt；服务端校验档位不可被模型上调 | 仅展示与回传，禁止本地改档位绕过校验 |
| 面试官流式追问 | 快模型流式输出（SSE），首字 < 2s | 逐字上屏、光标态、超时保留输入 |
| 教练实时建议 | 指标 90% 前 / 后端正则现算；LLM 仅补 1–3 条定性建议 | 正则统计填充词 / 句长 / 停顿 + 阈值色渲染 |
| 分析师结案报告 | 强模型事后生成（< 10s），与对话流隔离 | 触发后展示覆盖层，不阻塞对话（AC-11） |
| 安全护栏 | 后端校验模型永不越权加压；前端状态机（暂停 / 降级 / 结束） | 暂停=停追问、降级=切温和注入放缓、结束=触发报告 |
| 超长会话压缩 | 后端对 >40 轮历史做总结压缩后传人模型 | 无感提示，保持角色一致性 |
| 隐私 by design | 不存原始音频；对话 / 指标存 localStorage | UI 可见「本地处理 / 可删除」标识，报告本地导出（AC-12） |

**关键约束 UI 落地**：单轮单角色（面试官生成期间禁用用户发送，避免多角色污染）；模型分工隔离（训练区与报告覆盖层为独立视图）；本地优先（报告 / 指标 / 对话历史存 localStorage，提供「数据管理」入口）。本期不预留语音 ASR / SER / 微表情入口，输入框纯文本。

---

> 本文档为前端开发直接可用规范。下一步建议：将第 4 章线框落地为 **HTML/CSS 原型（浅色优先 + 双栏训练界面 + 深青绿强调色）**，或拆解为前端组件开发任务清单。
