# MindForge（心智锻造）技术方案 · AI 技术选型

> 定位：面向高压场景（考研复试 / 校招面试）的「心理韧性与表达力训练器」Web 端
> 作者视角：个人开发者 · 预算低 · 时间紧 · 数周内出可演示 MVP
> 配套文档：`MindForge_产品需求验证报告.md`
> 优先级约束（铁三角）：**学习成本低 → 开发效率高 → 成本可控（免费 / 极低价）**

---

## 0. TL;DR 一句话结论与推荐栈

**用「单模型 + 多角色提示词 + 前后端正则统计」在一个轻量全 JS 栈上跑通核心闭环，重型框架与多模态全部后置。**

| 层 | 推荐方案 | 备选 | 为什么 |
|---|---|---|---|
| 前端框架 | **Vue 3 + Vite**（中文文档友好、心智负担低） | React + Vite | 国内教程最全、上手最快、Chat UI 生态成熟 |
| 后端 | **Node.js + Express**（与前端同语言，零切换）或 **Serverless 函数**（Vercel/Cloudflare，免运维） | Python + FastAPI | 一个 JS 通吃前后端，API Key 不落地前端 |
| 大模型 API | **DeepSeek V4 Flash**（生产主力，闲时极廉价）+ **Qwen 百炼**（开发期 7000 万免费额度） | Kimi / 混元 | 中文强、便宜、1M 上下文装得下"黑板" |
| 多角色编排 | **单模型多角色提示词**（一份 transcripts + 三套 system prompt） | CrewAI / LangGraph | v1 完全不需要 Agent 框架，提示词即可 |
| 填充词指标 | **前端正则统计**（零推理成本） | LLM 打标 | 可在用户输入瞬间出指标，实时无延迟 |
| 安全护栏 | **前端状态机按钮 + 后端校验 + 教练 LLM 兜底** | 规则引擎 | 用户随时 暂停/降级/结束，模型永不越权加压 |
| 语音输入(v1 止步) | **Web Speech API**（浏览器原生、免费、中文可用） | 重型 ASR | 免费但限 Chromium；原始音频不上传，隐私友好 |

---

## 1. 前端框架对比与推荐

| 方案 | 学习成本 | 开发效率 | 生态（Chat/流式） | 备注 |
|---|---|---|---|---|
| **Vue 3 + Vite + Naive UI / Element Plus** | ★ 最低 | ★★★ | 极好，中文社区最强 | **推荐**。`<script setup>` 心智简单，组件库开箱即用 |
| React + Vite + shadcn/ui | 中 | ★★★ | 极好 | 招聘市场需求大但 JSX + hooks 对新手略陡 |
| Svelte / SvelteKit | 低 | ★★★ | 一般 | 优雅但中文资料少、招人难 |
| 原生 HTML/JS | 最低 | ★ | 无 | MVP 演示够用，但状态管理会反噬 |

**结论**：选 **Vue 3 + Vite**。理由：① 中文文档/教程/问答覆盖度最高，个人开发者卡住时最容易搜到答案；② `<script setup>` + 组合式函数（composable）写"对话流"很直观；③ `Naive UI` 一套组件搞定聊天界面、压力档位滑块、报告卡片，省去大量样式时间；④ 与 Node 后端同语言，无上下文切换。

**MVP 前端最小集**：
- 一个对话主区（面试官气泡 + 用户气泡，流式逐字上屏）
- 右侧教练面板（实时填充词/停顿指标 + 文字建议）
- 顶部：场景选择 + 三档施压（温和/标准/高压）+ 暂停/降级/结束按钮
- 结束页：分析师成长报告卡片

---

## 2. 后端框架对比与推荐

| 方案 | 运维负担 | 学习成本 | 与前端协同 | 成本 | 备注 |
|---|---|---|---|---|---|
| **Node.js + Express（单进程）** | 中（要起服务） | ★ 最低（同 JS） | 最好 | 免费（自托管） | **推荐**。一个文件代理 LLM，藏好 API Key |
| **Serverless 函数（Vercel/Cloudflare Workers）** | 零（平台托管） | 低 | 好 | 免费额度充足 | 不想管服务器时的首选，部署即上线 |
| Python + FastAPI | 中 | 中（第二语言） | 好 | 免费 | LLM 调用生态好，但你要同时学两种语言 |
| 直连前端调 LLM | 零 | 零 | — | — | **禁止**：API Key 会暴露给用户，等于送钱 |

**结论**：**Node + Express 薄后端**承载三件事：① 代理 LLM 调用并**保密 API Key**；② 做多角色编排（决定什么时候用哪套 prompt、是否流式）；③ 安全护栏的服务端校验（压力档位不能越权、单次时长上限）。部署建议：**Vercel / Cloudflare Pages + Functions** 免费层即可，无需买服务器；若不想碰 Serverless，用一台 ¥5/月 轻量 VPS 跑 Express 也行。

> 铁律：API Key 永远只在服务端，前端只持有一个"调用你自己后端"的地址。

---

## 3. 大模型 API 对比与推荐（2026-08 实测行情）

> ⚠️ 大模型价格 2026 年波动剧烈（国产集体涨价、峰谷分时计价），以下为 2026-08 快照，**上线前请以官方价格页为准**。

| 模型 | 输入(未命中)/百万token | 输出/百万token | 免费额度 | 特点 |
|---|---|---|---|---|
| **DeepSeek V4 Flash** | 闲时 ¥1.5 / 高峰 ¥3.0（缓存命中低至 ¥0.05–0.10） | 闲时 ¥4.5 / 高峰 ¥9.0 | 有活动额度，常较低 | **推荐主力**。1M 上下文、中文口癖少、便宜；闲时调用≈白送 |
| **DeepSeek V4 Pro** | 闲时 ¥4.5 / 高峰 ¥9.0 | 闲时 ¥13.5 / 高峰 ¥27 | — | 复杂推理/Agent 强，但 v1 用不上，留作分析师深度报告 |
| **Qwen 百炼 Qwen3.7-Flash** | ¥0.8 | ¥2.4 | **新用户最高 7000 万 Tokens（90 天）** | **开发期零成本首选**。高校师生再赠 3000 万 |
| Qwen3.7-Plus / Max | ¥1.5/¥2.5 入，¥4.5/¥7.5 出 | 同上 | 同上 | 质量更高，付费后按需升级 |
| Kimi / 混元 等 | 中等 | 中等 | 各有 | 长上下文备选 |

**推荐策略（分两阶段，零/极低成本）**：
1. **开发期（0 元）**：用 **Qwen 百炼新用户 7000 万免费额度**把 MVP 全部跑通、做演示，不花一分钱。
2. **上线期（极低价）**：切到 **DeepSeek V4 Flash**，**只走闲时（9:00–12:00、14:00–18:00 之外）** 调用把成本压到极限；缓存命中输入可低至 ¥0.05/百万。

**单模型承担三角色是否够？** 够。三个角色（面试官/教练/分析师）本质是同一基座模型换不同 system prompt。v1 不需要为每个角色单独模型——用一个 Flash 级模型 + 三套提示词即可，质量足够，成本最低。

---

## 4. 多角色编排方式对比与推荐

| 方案 | 工程复杂度 | 角色一致性 | 成本 | v1 适配度 |
|---|---|---|---|---|
| **单模型多角色提示词**（推荐） | ★ 最低 | 靠 prompt + 共享黑板保证 | 最低 | ★★★ 完全够 |
| CrewAI（角色驱动） | 中 | 好 | 中（多轮调用） | 过度设计 |
| LangGraph（状态机） | 高 | 最好（强约束） | 中 | 规模大/长会话再上 |
| AutoGen（GroupChat） | 中 | 中 | 高（多 Agent 对话） | 偏多 Agent 自治 |

### 4.1 推荐方案：单模型 + 共享"黑板" + 三套 Prompt

**架构核心**：一份不断增长的 `transcript`（对话黑板），三个角色都读它、写它，但各自用不同的 system prompt 引导。没有 Agent 框架，没有消息总线，就是一个 JSON 数组。

```
transcript = [
  { role: "system_interviewer", content: "…", ts: … },
  { role: "user",               content: "…", ts: … , metrics: {filler, pause, …} },
  { role: "interviewer",        content: "…", ts: … },
  { role: "coach",              content: "…", ts: … },   // 侧栏，不进主对话流
  …
]
```

**三个角色职责与触发**：
- **面试官（interviewer）**：用户每发一条 → 服务端用「面试官 system prompt + 当前 transcript + 压力档位」调用 LLM，**流式**返回，逐字上屏。负责动态追问与可调施压。
- **教练（coach）**：用户发完一条 → **异步、非流式**快速调用（也可部分用正则替代），把"表达力反馈"写入侧栏，不影响主对话节奏。**v1 的教练指标 90% 来自前端正则**（见第 5 节），LLM 只补一句定性建议，省 token。
- **分析师（analyst）**：用户点「结束」→ 服务端把整段 transcript + 累计指标一次性发给「分析师 system prompt」，**批处理**生成结构化 JSON 报告（语言组织/逻辑清晰度/情绪稳定性/改进建议）。分析师可用更强的模型（如 DeepSeek V4 Pro 或 Qwen Max）深算——因为不是实时，成本可控。

**面试官提示词模板（含压力档位变量）**：
```
你是一位严格的{{scenario}}面试官，正在对考生进行高压模拟训练。
当前施压强度：{{pressure}}（gentle=温和 / standard=标准 / high=高压）。
- gentle：语气平和，追问温和，多给正向鼓励。
- standard：专业、平稳，适度追问与质疑。
- high：冷峻、连续追问、制造沉默压力、偶尔打断，但绝不人身攻击、绝不侮辱。
规则：
1. 始终记住你是"训练工具"，目标是帮助用户脱敏，不是真的淘汰他。
2. 严禁跳出人设去安慰或闲聊；如需安抚由"教练"角色负责。
3. 每次只问一个问题，保持高压对话节奏。
4. 不得提供标准答案，只引导用户自己表达。
以下是到目前为止的对话记录：
{{transcript}}
```

**角色一致性保障（轻量三招，不引入框架）**：
1. 显式 system prompt + "禁止跳出人设"护栏（如上）。
2. 共享同一份 transcript 作为黑板，教练/分析师看到的就是面试官看到的，避免信息错位。
3. 分析师前加一层"角色校验"：若 transcript 里出现面试官破功（如开始安慰），在分析师 prompt 里显式标注"忽略面试官失态，仅基于用户表现评估"。

> 何时才需要 LangGraph？只有当"长会话人设被带偏、多 Agent 互相污染"成为真实瓶颈（通常是用户量上来、会话 >30 轮之后）再迁移。v1 用提示词完全跑得通。

---

## 5. 填充词指标方案（前端正则统计，零推理成本）

**原则**：v1 的表达力指标 **全部用前端正则 + 时间戳统计**完成，用户输入的瞬间即可出数，零延迟、零 token。LLM 只做"一句话定性建议"的点缀。

### 5.1 指标清单与算法

| 指标 | 计算方式 | 数据源 |
|---|---|---|
| 填充词数量 / 占比 | 正则命中数 ÷ 总字数 | 用户文本 |
| 重复词 | 相邻词语重复检测（正则/滑动窗口） | 用户文本 |
| 停顿次数 / 平均停顿 | 相邻两条用户消息时间戳差 > 阈值(如 3s) 计一次 | 消息 ts |
| 语速估算 | 字数 ÷（首条 interim 开始 → 本条 final 结束的时长） | Web Speech 时间戳 |
| 句子完整度 | 以句号/问号结尾比例；长句占比 | 用户文本 |
| 情绪波动信号 | 关键词命中外露紧张/回避（见下） | 用户文本 |

### 5.2 前端正则词典（示例，可扩展）

```js
// 填充词（口语赘词）词典
const FILLER = [
  '嗯','啊','呃','额','哦','呀',
  '那个','这个','就是说','然后','其实','基本上','怎么说呢',
  '你知道吧','对吧','可能吧','好像','大概','就是','的话',
  'em','um','uh','like','you know'
];
const FILLER_RE = new RegExp(`(${FILLER.join('|')})+`, 'gi');

// 外露紧张/回避信号（教练兜底用）
const DISTRESS_RE = /(我不行了|我受不了|想退出|太难了|放弃|崩了|焦虑|紧张死)/;

function analyzeText(text, prevTs, currTs) {
  const fillers = (text.match(FILLER_RE) || []).length;
  const chars   = text.replace(/\s/g, '').length;
  const fillerRate = chars ? fillers / chars : 0;
  const pause = (prevTs && currTs) ? (currTs - prevTs) / 1000 : 0;
  return { fillers, fillerRate, pause };
}
```

### 5.3 实时展示（侧栏颜色阈值）

- 填充词率 `<5%` 绿 / `5–12%` 黄 / `>12%` 红
- 平均停顿 `<2s` 绿 / `2–4s` 黄 / `>4s` 红
- 教练面板每轮刷新，并用一句话定性（如"你用了 6 次'然后'，显得思路被打断，试着用'第一/第二'分点"）——这句可由正则直接模板生成，省一次 LLM 调用。

---

## 6. 安全护栏实现（体验底线，必须做）

**设计哲学**：施压是"可调节的训练强度"，不是伤害。用户永远拥有最高控制权；模型**永不**自主突破用户选择的档位上限。

### 6.1 前端状态机

```
pressure ∈ {gentle, standard, high}     // 用户选定，不可被模型上调
phase    ∈ {active, paused, ended}
```

- **暂停**：`phase = paused` → 停止面试官追问，界面显示呼吸引导/安抚语，用户可随时恢复。
- **降级**：`pressure = gentle` → 立即把面试官提示词切到温和档，并注入"刚才强度过高，现在放缓"。
- **结束**：`phase = ended` → 触发分析师生成报告，会话归档。
- **恐慌词兜底**：用户输入命中 `DISTRESS_RE` 或教练 LLM 判定情绪极差 → 前端弹窗"需要我降低强度吗？"并一键降级/暂停，同时展示支持资源文案（非医疗建议）。

### 6.2 服务端校验（防越权）

- 服务端接收请求时校验 `pressure` 必须 ≤ 用户会话初始选择，**拒绝任何上调请求**（即便前端被篡改）。
- 硬性上限：单次会话最长时长（如 20 分钟）、最大轮次（如 40 轮），到点自动降级并提示休息。
- 面试官输出做"毒性/侮辱"后筛：若模型输出含侮辱性内容（正则 + 轻量审核），丢弃并 Fallback 到温和模板。

### 6.3 教练兜底安抚

- 教练角色在侧栏始终给出"建设性 + 支持性"语气，与面试官的冷峻形成对冲，避免用户被单方面压制。

---

## 7. 应后置的能力清单（明确版本规划）

以下能力 v1 **不做**，用轻量替代跑通核心闭环，待 PMF 后再投入：

| 能力 | 为何后置 | v1 的轻量替代 | 建议版本 |
|---|---|---|---|
| **实时 ASR（Whisper/Paraformer 带时间戳）** | 需后端服务/付费、工程重 | **Web Speech API**（浏览器原生、免费、中文可用，限 Chromium）做语音输入 | v1 即用 Web Speech；重型 ASR 留 v2 |
| **语音情绪识别 SER（SenseVoice 等）** | 准确率中等、公平性受质疑、需音频管线 | v1 用**文本关键词 + 填充词/停顿指标**近似"情绪稳定性" | v2/v3 |
| **微表情视频分析** | 隐私敏感 + 技术未成熟 + 公平性风险 | 不做（与需求报告结论一致） | v3+ 或不做 |
| **账号 / 支付** | 早期无留存需求、增加合规负担 | v1 **无账号**，数据存 `localStorage`，报告可导出/分享图 | v2（赛季制付费时） |
| **重型多智能体框架（CrewAI/LangGraph/AutoGen）** | 过度工程、学习成本高 | **单模型多角色提示词 + 共享黑板**（本文方案） | 仅当角色一致性成瓶颈时上 LangGraph |
| **向量库 / 长期记忆** | v1 单会话即可 | transcript 内存 + localStorage；跨场成长追踪留 v2 | v2 |

> 说明：Web Speech API 的 SpeechRecognition 实际会把音频发往浏览器厂商云端识别（非完全本地），**免费但非完全隐私**。对训练类工具 v1 可接受；对声纹隐私敏感的用户，v2 再换本地化/可删处理的方案。

---

## 8. 轻量栈核心闭环（数据流）

```
用户文本/语音 ──▶ [前端] 正则统计(填充词/停顿/语速)
                         │
                         ├─▶ 侧栏：实时指标 + 教练定性建议（正则模板，≈0 成本）
                         │
                         ▼
                   [Node/Express 后端]
                   ① 校验 pressure/phase（防越权）
                   ② 拼 transcript（共享黑板）
                   ③ 调 LLM：面试官 system prompt + transcript + 压力档位
                         │
                         ▼
                   [LLM: DeepSeek V4 Flash / Qwen]
                   流式返回面试官追问 ──▶ 前端逐字上屏
                         │
                   [用户点"结束"]
                         ▼
                   ④ 调 LLM：分析师 system prompt + 全量 transcript + 累计指标
                         ▼
                   结构化成长报告 JSON ──▶ 前端报告卡片（可导出）
```

**闭环跑通的关键**：面试官(流式) + 教练(前端正则) + 分析师(批处理) 三者共享同一份 transcript，全程**一个模型、三套提示词、零 Agent 框架**。

---

## 9. 实施路线图（数周出 MVP）

| 周次 | 目标 | 产出 |
|---|---|---|
| W1 | 脚手架：Vue3+Vite+Naive UI + Express 薄后端；打通 LLM 流式 | 能对话的"面试官"原型 |
| W2 | 三档施压 + 共享黑板 + 教练侧栏（正则指标） | 核心训练闭环可演示 |
| W3 | 安全护栏（暂停/降级/结束 + 服务端校验）+ 分析师报告 | 完整 MVP |
| W4 | Web Speech 语音输入（Chromium）、报告导出、打磨演示 | 可对外演示版本 |

---

## 10. 成本测算（个人开发者视角）

| 阶段 | 费用 | 说明 |
|---|---|---|
| 开发期 | **¥0** | Qwen 百炼 7000 万免费额度足够跑通 + 演示 |
| 上线期(闲时) | 约 **¥0.03–0.1 / 次会话** | DeepSeek V4 Flash 闲时输出 ¥4.5/百万token；单场 ~5–10k token 输出 |
| 前端托管 | **¥0** | Vercel / Cloudflare 免费层 |
| 后端托管 | **¥0**（Serverless）或 ¥5/月（轻量 VPS） | 流量小可长期免费 |
| 语音输入 | **¥0** | Web Speech API 原生免费 |

**结论**：在免费额度 + 闲时调用策略下，MVP 阶段几乎零成本；即便收费后，单用户单场训练成本也远低于一杯水。

---

## 11. 风险与备选

- **模型涨价风险**：2026 国产模型集体涨价，已用"闲时 + 缓存命中 + Qwen 免费额度兜底"对冲；必要时切更便宜模型（Kimi/混元）或本地轻量模型。
- **Web Speech 兼容性**：仅 Chromium 完整支持 → MVP 明确"推荐 Chrome/Edge"，并提供纯文本输入兜底。
- **角色一致性**：若长会话破功，先加 prompt 护栏；仍不行再上 LangGraph（已在第 4 节留好迁移路径）。
- **隐私**：v1 不存原始音频、不强制账号；声纹/情绪等生物特征相关能力一律后置并默认"可删除"。

---

## 12. 一句话给开发者的行动清单

1. `npm create vue@latest` + 装 `naive-ui`；后端 `express` 一个文件代理 LLM。
2. 先用 **Qwen 百炼免费额度** 把面试官流式对话跑通。
3. 加「三档施压」变量进 system prompt，用一份 `transcript` 当黑板。
4. 教练指标用**前端正则**（第 5 节词典）实时出，别急着调模型。
5. 暂停/降级/结束做成前端状态机 + 后端校验（第 6 节）。
6. 结束触发分析师 prompt 出 JSON 报告。
7. 语音先用 **Web Speech API**（Chromium），重型 ASR/SER/微表情/账号支付全部后置。

> 守住定位：**不拼"面试仿真"，拼"心理脱敏 + 表达力教练"**。轻量栈足以把"模拟"真正变成"训练"。
